var nome = "Caneta";
var quantidade = 10; //variavel armazena a informação. quantidade é nome da variável e 10 é o valor
var preco = 6.4;
let imposto = 1.5;
let precoFinal = preco + imposto;

console.log(nome);
console.log(quantidade);
console.log(preco);  // valores decimais são separados da maneira americana, com ponto ao invés de vírgula
console.log(imposto);  //valor literal, digitado sem definir o que é
console.log(precoFinal);

// let é o mais utilizado para criar variável;
//let e var são variáveis distintas;

//teste de alteração  sempre salva